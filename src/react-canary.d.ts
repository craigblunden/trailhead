/*
 * `ViewTransition` ships in the React the App Router bundles, but its types are still in the canary
 * declarations. This makes `import { ViewTransition } from "react"` typecheck.
 */
/// <reference types="react/canary" />
