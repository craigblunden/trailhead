/** The product's name as it appears everywhere a person reads it: wordmark, titles, copy. */
export const BRAND_NAME = "Trail to Offer";

/** Where the product is served in production: for absolute links read outside the app, such as `/llms.txt`. */
export const SITE_URL = "https://www.trailtooffer.com";
